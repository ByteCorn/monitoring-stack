x-logging-gelf: &gelf-logging
  logging:
    driver: gelf
    options:
      gelf-address: "udp://172.17.0.1:12201" # Graylog порт из стека логирования
      tag: "monitoring-{{.Name}}" # автоматически добавит имя контейнера в тег

services:

  prometheus:
    <<: *gelf-logging
    image: prom/prometheus:latest
    user: root
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./alerts.yml:/etc/prometheus/alerts.yml:ro
      - ./prometheus_data:/prometheus
    ports:
      - "9090:9090"
    links:
      - alertmanager:alertmanager
    restart: unless-stopped

  alertmanager:
    <<: *gelf-logging
    image: prom/alertmanager:latest
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
    volumes:
      - ./alertmanager.yml.tpl:/etc/alertmanager/alertmanager.yml.tpl:ro
      - ./scripts/start-alertmanager.sh:/start-alertmanager.sh:ro
    ports:
      - "9093:9093"
    restart: unless-stopped

  node-exporter:
    <<: *gelf-logging
    image: quay.io/prometheus/node-exporter:latest
    pid: host
    command:
      - '--path.procfs=/host/proc'
      - '--path.rootfs=/rootfs'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    restart: unless-stopped

  dcgm-exporter:
    <<: *gelf-logging
    image: nvcr.io/nvidia/k8s/dcgm-exporter:latest
    environment:
      - DCGM_EXPORTER_NO_HOSTNAME=1
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
    ports:
      - "9400:9400"
    cap_add:
      - SYS_ADMIN
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
    restart: unless-stopped
